{
  "buildCommand": "npx --yes patch-package --patch-dir ./ && DISABLE_ESLINT_PLUGIN=true CI=false npm run build",
  "outputDirectory": "build",
  "installCommand": "npm install --legacy-peer-deps --omit=optional",
  "build": {
    "env": {
      "NODE_VERSION": "16",
      "DISABLE_ESLINT_PLUGIN": "true",
      "CI": "false"
    }
  }
}